[target.nanosplus]
runner = "speculos -a=1 --model=nanosp"

[build]
target = "nanosplus"

[env]
HEAP_SIZE = "14306"

[unstable]
build-std = ["core", "alloc"]
build-std-features = ["compiler-builtins-mem"]
